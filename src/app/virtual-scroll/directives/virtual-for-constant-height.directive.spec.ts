import { VirtualForConstantHeightDirective } from './virtual-for-constant-height.directive';

describe('VirtualForConstantHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new VirtualForConstantHeightDirective();
    expect(directive).toBeTruthy();
  });
});
