import { VirtualForDirective } from './virtual-for.directive';

describe('VirtualForDirective', () => {
  it('should create an instance', () => {
    const directive = new VirtualForDirective();
    expect(directive).toBeTruthy();
  });
});
